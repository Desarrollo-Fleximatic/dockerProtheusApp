# Use postgres/example user/password credentials
version: '3.1'

services:

  api:
    image: desarrollofleximatic/protheusapp
    container_name: protheusapp
    ports:
      - '49165:80'
    restart: always